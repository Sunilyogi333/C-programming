/*
Static Initialization of Variable
-If the initialization of the variable is done at the time of variable declaration or compile time, 
it is called static initialization of the variable.
for example: int a=5
*/
#include<stdio.h>
int main()
{
    int a = 5;
    printf("The value of a is %d\n", a);
}