#include <stdio.h>

int main()
{
    int l, w;
    printf("Type the length of rectangle \n");
    scanf("%d", &l);

    printf("Type the width of rectangle \n");
    scanf("%d", &w);

    printf("Area of rectangle %d", l * w);
    return 0;
}