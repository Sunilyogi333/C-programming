/*
for loop
syntax:
for(initialization; condition; updation)
{
    loop body(C code);
}
*/
#include<stdio.h>

int main()
{
    
    for(int a = 0 ; a<10; a++ )
    {
        printf("The value of a is %d \n", a);
    }
    return 0;
}